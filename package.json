{
  "name": "devsonar-monorepo",
  "version": "0.1.3",
  "private": true,
  "description": "AI-powered error monitoring system for local development",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  },
  "packageManager": "npm@11.2.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
